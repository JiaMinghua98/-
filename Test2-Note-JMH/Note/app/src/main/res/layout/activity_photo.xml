<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_photo"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.dell.note.PhotoActivity">

        <Button
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:id="@+id/buttonTakePhoto"
            android:text="Take Photo"/>


        <ImageView
            android:layout_width="fill_parent"
            android:layout_height="1200px"
            android:id="@+id/photoThumbnail"
            android:background="#efefef"
            android:layout_below="@id/buttonTakePhoto"/>


        <Button
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:id="@+id/buttonTakePhotoAndSaveToPublic"
            android:layout_below="@id/photoThumbnail"
            android:text="拍照并保存至相册" />

</RelativeLayout>
